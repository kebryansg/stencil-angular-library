{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:pichincha": "lerna run --scope stencil-library build",
    "serve:customer": "lerna run --ignore stencil-library --ignore ds-pichincha-angular start",
    "build:ds-angular": "lerna run --ignore stencil-library build"
  },
  "devDependencies": {
    "@stencil/angular-output-target": "^0.7.1",
    "@types/node": "^20.4.6",
    "lerna": "^7.1.4",
    "typescript": "^5.1.6"
  }
}
